

// character input and output:
#include <stdio.h>

void main() {
    
    // reads next input from the character stream and returns it as its value
    char c=getchar();

    // prints a charcater each time it is called
    
    putchar(c);
    
    
    
    return;
}